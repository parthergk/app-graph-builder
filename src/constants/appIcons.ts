import {
  Layers,
  LineChart,
  ShoppingCart,
  Box,
  Bell,
} from "lucide-react";

export const appIcons = {
  layers: Layers,
  chart: LineChart,
  cart: ShoppingCart,
  box: Box,
  bell: Bell,
} as const;